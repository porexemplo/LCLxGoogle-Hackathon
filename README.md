# hackathon2024

## Install dependencies:
`cd front && npm install && cd ../back && npm install`

## Execution :
### Front side :
`cd front`
`npm run serve`

### Back side :
`cd back`
`node express.js`

